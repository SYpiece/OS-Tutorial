*在开始之前你可能需要 Google 了解的概念：中断、CPU寄存器*

**目标：让之前的引导扇区输出一些文本**

我们对无限循环的引导扇区做点改进，让它能够打印点东西在屏幕上。我们通过发出中断来完成这个功能。

在这个例子中，我们会把 “Hello” 这个词的每个字符写入寄存器 `al`（`ax` 的低端部分），并将字节 `0x0e` 写入 `ah`（`ax` 的高端部分），并引发中断 `0x10`，这是一个视频服务的通用中断。

`ah` 上的 `0x0e` 告诉视频中断，我们想要以终端模式打印 `al` 的内容”。

不过，在实际操作中，我们只能设置一次终端模式，因为我们无法确定 `ah` 的内容是恒定的。在我们休眠时，其他进程可能在CPU上运行，没有正确清理并在 `ah` 上留下的垃圾数据。

对于这个例子，我们不需要考虑这个问题，因为我们是唯一在CPU上运行的程序。

我们的新引导扇区如下所示：
```nasm
mov ah, 0x0e ; 终端模式
mov al, 'H'
int 0x10
mov al, 'e'
int 0x10
mov al, 'l'
int 0x10
int 0x10 ; 'l' 仍在 al 中，记得吗？
mov al, 'o'
int 0x10

jmp $ ; 跳转到当前地址 = 无限循环

; 填充和魔术数字
times 510 - ($-$$) db 0
dw 0xaa55 
```

你可以使用 `xxd file.bin` 检查二进制数据

当然你已经知道我们的编译和运行步骤：

`nasm -fbin boot_sect_hello.asm -o boot_sect_hello.bin`

`qemu boot_sect_hello.bin`

你的引导扇区将显示 “Hello” 并挂在无限循环上